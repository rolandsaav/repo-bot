<html>

<body>
<form action="http://localhost:3000/logout" method="post" >
    <button type="submit">Logout</button>
</form>
<%if (repos.length > 0) { %> 
    <% repos.forEach(repo => { %>
        <p><%=repo %></p>
    <% }) %>
<%} %>
</body>

</html>
